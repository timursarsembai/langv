# DEV_LOG — LangV Player

> Журнал разработки: записывай каждую решенную проблему в формате:
> **Проблема → Причина → Решение**

---

## 📅 2026-01-09 — Инициализация проекта

### ✅ Шаг 0: Настройка проекта и правила

**Технологический стек:**
- Платформа: .NET 8 (WPF)
- Видео-движок: Mpv.NET
- База данных: Microsoft.Data.Sqlite
- Сериализация: Newtonsoft.Json
- Экспорт в Anki: AnkiNet

**Правила разработки:**
- Комментарии в коде: английский + русский
- Ответы ассистента: русский язык
- Интерфейс: темная тема, без стандартных рамок окна

---

## 🐛 Решенные проблемы

### Проблема: Не найдены NuGet пакеты при установке
- **Причина:** Отсутствовал официальный источник nuget.org в конфигурации NuGet
- **Решение:** `dotnet nuget add source https://api.nuget.org/v3/index.json -n nuget.org`
- **Дата:** 2026-01-09

### Проблема: Конфликт типов WPF и WinForms
- **Причина:** При включении `UseWindowsForms` для хостинга MPV возникли конфликты имен (Panel, OpenFileDialog, KeyEventArgs, Application)
- **Решение:** Явное указание пространства имен (`System.Windows.Forms.Panel`, `Microsoft.Win32.OpenFileDialog`, `System.Windows.Input.KeyEventArgs`, `System.Windows.Application`)
- **Дата:** 2026-01-09

### Проблема: Mpv.NET не совместим с новой версией mpv
- **Причина:** Функция `mpv_detach_destroy` была удалена из API libmpv в новых версиях (заменена на `mpv_destroy`)
- **Решение:** Переход на LibVLCSharp + VideoLAN.LibVLC.Windows как более стабильную альтернативу
- **Дата:** 2026-01-09

### Проблема: После окончания видео нельзя воспроизвести заново
- **Причина:** VLC переходит в состояние Ended, и простой вызов Play() не работает
- **Решение:** Проверка VLCState.Ended в TogglePlayPause() и перезагрузка медиа через LoadVideo(_currentVideoPath)
- **Дата:** 2026-01-09

### Проблема: Полноэкранный режим только через кнопку
- **Причина:** Отсутствовал обработчик двойного клика на области видео
- **Решение:** Добавлен VideoArea_MouseLeftButtonDown с проверкой e.ClickCount == 2 для вызова ToggleFullscreen()
- **Дата:** 2026-01-09

### Проблема: Панель управления не появляется при наведении в полноэкранном режиме
- **Причина:** Не было логики для отслеживания движения мыши и показа/скрытия панели
- **Решение:** Добавлен DispatcherTimer _controlPanelTimer с 3-секундным интервалом, обработчик VideoArea_MouseMove для показа панели и сброса таймера
- **Дата:** 2026-01-09

### Проблема: LibVLCSharp WPF Airspace — мышь не работает поверх видео
- **Причина:** WPF Airspace issue — WPF элементы не получают события мыши поверх WinForms контрола VideoView
- **Решение:** Прозрачный Grid overlay ВНУТРИ VideoView с Background="#01000000" (почти прозрачный, но не полностью) по документации LibVLCSharp
- **Дата:** 2026-01-09

### Проблема: Пробел активирует последнюю нажатую кнопку вместо паузы
- **Причина:** Кнопки получают фокус при клике, и Пробел срабатывает как их активация
- **Решение:** Добавлен Focusable="False" для всех кнопок, слайдеров и ToggleButton
- **Дата:** 2026-01-09

### Проблема: Клик по слайдеру таймлайна не перемещает бегунок
- **Причина:** Стандартное поведение Slider — только перетаскивание
- **Решение:** Добавлен IsMoveToPointEnabled="True" и расчет позиции при PreviewMouseDown
- **Дата:** 2026-01-09

### Проблема: Виджеты Windows 11 видны в полноэкранном режиме
- **Причина:** WindowState.Maximized не перекрывает виджеты и панель задач полностью
- **Решение:** Ручное позиционирование окна через Screen.FromHandle().Bounds с Topmost=true
- **Дата:** 2026-01-09

### Проблема: Горячие клавиши не работают после клика по области видео
- **Причина:** VideoView (WinForms контрол) захватывает фокус
- **Решение:** Добавлен this.Focus() при клике по VideoOverlay
- **Дата:** 2026-01-09

### Проблема: Меню выглядит как Windows 95 с белыми границами
- **Причина:** Стандартные стили WPF Menu/MenuItem
- **Решение:** Полный ControlTemplate для MenuItem с тёмной темой, закруглёнными углами, тенью и плавной анимацией
- **Дата:** 2026-01-09

### Проблема: Нет звука в AVI файлах с AC3 аудио
- **Причина:** NuGet пакет VideoLAN.LibVLC.Windows не включает декодер AC3/A52 из-за патентных ограничений Dolby
- **Решение:** Использование полной версии VLC Media Player (Core.Initialize(@"C:\Program Files\VideoLAN\VLC")) вместо NuGet пакета
- **Дата:** 2026-01-09

### Проблема: Перемотка по клику нестабильна — позиция возвращается назад
- **Причина:** Race condition — таймер обновления позиции перезаписывает значение слайдера до завершения seek операции VLC
- **Решение:** Отслеживание ожидаемой позиции `_pendingSeekPosition` и блокировка обновлений пока `Math.Abs(currentTime - _pendingSeekPosition) < SeekToleranceMs` (2000мс). Флаг `_isSeekPending` сбрасывается только когда VLC достигает целевой позиции.
- **Дата:** 2026-01-09

### Проблема: Pin (Всегда поверх) не применяется при загрузке приложения
- **Причина:** WindowHelper.SetAlwaysOnTop вызывался в Window_Loaded, когда hwnd мог быть ещё не готов (IntPtr.Zero)
- **Решение:** Перенос применения AlwaysOnTop в событие `SourceInitialized` в конструкторе, где hwnd гарантированно создан
- **Дата:** 2026-01-09

### Проблема: Перемотка по клику всё ещё нестабильна (v2)
- **Причина:** Предыдущее решение с polling таймером (DispatcherTimer 250мс) создавало race condition — таймер мог обновить слайдер старым значением до завершения seek операции VLC
- **Решение:** Полная переработка — убран polling таймер `_positionTimer`, используется событие `TimeChanged` от VLC MediaPlayer. Теперь обновление слайдера происходит только когда VLC сам сообщает о новой позиции. Блокировка обновлений только по флагу `_isDraggingSlider`.
- **Дата:** 2026-01-09

### Проблема: Перемотка по клику нестабильна (v3) — левый клик возвращает позицию назад
- **Причина:** Конфликт между нашим `PreviewMouseDown` и встроенным поведением WPF Slider с `IsMoveToPointEnabled="True"`. Slider сам обрабатывает клик, начинает "drag" операцию, и при отпускании мыши сбрасывает значение. Также seek вызывался дважды (в MouseDown и MouseUp), что вызывало "рывки".
- **Решение:** 
  1. Добавлен `e.Handled = true` для предотвращения стандартной обработки Slider
  2. Используется `CaptureMouse()` / `ReleaseMouseCapture()` для корректного отслеживания drag
  3. Seek (`_mediaPlayer.Time`) вызывается только один раз в `MouseUp`
  4. Добавлен `PreviewMouseMove` для обновления слайдера во время перетаскивания
  5. Обработка только левой кнопки мыши (`e.ChangedButton == MouseButton.Left`)
- **Дата:** 2026-01-09

### Проблема: Перемотка кликом не работает (v4) — бегунок возвращается, кадр не меняется
- **Причина:** Между `MouseDown` и `MouseUp` событие `TimeChanged` от VLC успевало сбросить значение слайдера обратно к текущей позиции воспроизведения (Dispatcher.Invoke работает асинхронно).
- **Решение:**
  1. Добавлено поле `_seekTargetPosition` для сохранения целевой позиции перемотки
  2. В `MouseDown` сохраняем целевую позицию в `_seekTargetPosition`
  3. В `MouseUp` используем сохранённую `_seekTargetPosition` для seek (а не `TimelineSlider.Value`)
  4. В `TimeChanged` проверяем `_seekTargetPosition >= 0` для блокировки обновлений
  5. После seek сбрасываем `_seekTargetPosition = -1`
- **Дата:** 2026-01-09

### Проблема: Перемотка по клику нестабильна (v5 ФИНАЛЬНОЕ РЕШЕНИЕ)
- **Причина:** Конфликт между стандартным поведением WPF Slider (`IsMoveToPointEnabled`, внутренняя логика drag) и нашими обработчиками `PreviewMouseDown`. Slider перехватывал события и сбрасывал значения.
- **Решение:**
  1. Убран `IsMoveToPointEnabled="True"` из XAML
  2. Убран `PreviewMouseDown` из XAML
  3. Используется `AddHandler` с `handledEventsToo = true` для перехвата события `MouseLeftButtonDown` даже после внутренней обработки Slider
  4. Обработчик `TimelineSlider_TrackMouseDown` проверяет клик на thumb и пропускает его (thumb обрабатывается через `DragStarted/DragCompleted`)
  5. Используется `_mediaPlayer.Position` (0.0-1.0) вместо `_mediaPlayer.Time` для лучшей совместимости с MKV
  6. Блокировка обновлений через `_pendingSeekPosition` до достижения VLC целевой позиции
- **Дата:** 2026-01-09

### Проблема: Зависание при открытии нескольких файлов подряд
- **Причина:** Объект `Media` не освобождался при загрузке нового видео, что приводило к утечке памяти и накоплению ресурсов
- **Решение:**
  1. Добавлено поле `_currentMedia` для отслеживания текущего объекта Media
  2. В `LoadVideo()` вызывается `_mediaPlayer.Stop()` и `_currentMedia?.Dispose()` перед созданием нового Media
  3. В `Window_Closing()` добавлен `_currentMedia?.Dispose()` для освобождения при закрытии
  4. Сброс `_pendingSeekPosition = -1` при загрузке нового видео
- **Дата:** 2026-01-09

### Проблема: Горячие клавиши не работают после добавления плейлиста
- **Причина:** Когда фокус на ListBox плейлиста, он перехватывает клавишу Space (стандартное поведение — выделение элемента) раньше, чем окно успевает обработать `KeyDown`
- **Решение:**
  1. Заменён `KeyDown="Window_KeyDown"` на `PreviewKeyDown="Window_PreviewKeyDown"` в XAML
  2. Переименован обработчик `Window_KeyDown` → `Window_PreviewKeyDown`
  3. `PreviewKeyDown` — событие туннелирования, которое срабатывает на окне ДО дочерних элементов
- **Дата:** 2026-01-09

---
